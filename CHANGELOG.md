# 修改日志

以后每次修改项目，都需要在本文件中追加一条日志，包含时间戳和修改内容摘要。

## 2026-05-16 00:51:57 +08:00

- 调整发射球进入轨道的判断：发射球靠近轨道时会检查该轨道位置附近是否有球；如果最近轨道为空，会跳过该轨道并继续飞向后面的轨道候选，直到进入有球的轨道或直接命中球。
- 将轨道绘制改为平滑曲线路径函数，轨道主体、内层轨道和虚线纹理统一使用曲线绘制，减少折线感。

## 2026-05-16 00:46:09 +08:00

- 新增项目修改日志文件，并在 README 中写入后续修改必须同步更新日志的规范。
- 记录当前 `ai-zuma-game` 分支已有功能：祖玛风格网页消除游戏、6 色球、RGB/YUV 调色、3 关、暂停倒计时/回退队列/相邻消除 3 种技能。
- 记录上一轮修复：降低球链前进速度；发射球命中轨道或已有球后会插入球链，不再继续飞出屏幕；轨道改为 Catmull-Rom 平滑曲线采样。
